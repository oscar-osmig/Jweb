# Registers the framework's beans (controllers, WebSocket, config, dev tools)
# in any consuming application — so apps only component-scan their own package.
com.osmig.Jweb.framework.JWebAutoConfiguration
