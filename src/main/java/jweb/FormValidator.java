package jweb;

/**
 * Fluent form validation, started with {@code FormValidator.create()}.
 *
 * <p>Short alias for {@link com.osmig.Jweb.framework.validation.FormValidator} —
 * {@code create()} always hands out this subtype, so either name works in
 * declarations.</p>
 */
@SuppressWarnings("deprecation")
public class FormValidator extends com.osmig.Jweb.framework.validation.FormValidator {

    /** Internal — start with {@code FormValidator.create()}. */
    public FormValidator() {}
}
