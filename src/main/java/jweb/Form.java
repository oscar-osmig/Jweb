package jweb;

/**
 * Form building with validation:
 *
 * <pre>{@code
 * import static jweb.Form.*;
 * }</pre>
 *
 * <p>Short alias for {@link com.osmig.Jweb.framework.forms.Form} —
 * {@code create()} always hands out this subtype, so either name works in
 * declarations.</p>
 */
@SuppressWarnings("deprecation")
public class Form extends com.osmig.Jweb.framework.forms.Form {

    /** Internal — start with {@code Form.create()}. */
    public Form() {}
}
