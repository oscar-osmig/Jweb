# JitPack build config — JWeb needs JDK 21 (records, sealed types, virtual threads).
# Invoked via `sh` so the build never depends on the wrapper's file mode.
jdk:
  - openjdk21
install:
  - sh mvnw -B install -DskipTests
